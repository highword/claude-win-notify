---
id: "03-03"
title: "Integration Tests & End-to-End Verification"
phase: 3
wave: 2
depends_on: ["03-01"]
requirements:
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - TOAST-03
files_modified:
  - tests/notification_types.rs
  - src/assets.rs
autonomous: true
---

# Plan 03-03: Integration Tests & End-to-End Verification

## Objective

Add integration tests that verify the full classification pipeline and asset extraction work correctly end-to-end. These tests validate all 5 success criteria from the ROADMAP without requiring Windows Toast UI.

## Tasks

<task id="1">
<title>Integration tests for the full classification pipeline</title>
<read_first>
- src/notification.rs (classify_stop, classify_notification, body_text)
- src/hook.rs (HookInput struct)
- .planning/phases/03-notification-types/03-CONTEXT.md (D-13 through D-19, all classification boundaries)
</read_first>
<action>
Create `tests/notification_types.rs` (integration test file at crate root `tests/` directory):

```rust
use claude_win_notify::hook::HookInput;
use claude_win_notify::notification::{classify_stop, classify_notification, body_text, NotificationType};
```

Tests covering each ROADMAP success criterion:

**SC-1: Task Complete fires on Stop when task finishes successfully**
- `stop_normal_completion_is_task_complete` — Stop with "I've completed the implementation" → TaskComplete
- `stop_with_code_output_is_task_complete` — Stop with "Here's the updated code:\n```rust\nfn main() {}\n```" → TaskComplete

**SC-2: Permission Request fires when Claude needs tool approval**
- `notification_permission_prompt_type` — notification_type="permission_prompt" → PermissionRequest
- `notification_permission_fallback_when_type_missing` — notification_type=None, message="Claude wants permission to run Bash" → PermissionRequest (D-19 bug #11964)
- `notification_permission_case_insensitive` — message="PERMISSION needed" → PermissionRequest

**SC-3: Question fires when Claude asks a question**
- `stop_question_ends_with_question_mark` — "Would you like me to proceed?" → Question
- `stop_question_multiline_last_line` — "Here's the plan:\n1. Do X\n2. Do Y\nShall I proceed?" → Question (last line ends with ?)
- `notification_idle_prompt_is_question` — notification_type="idle_prompt" → Question (D-06)
- `notification_unknown_type_is_question` — notification_type="some_future_type" → Question (D-18)

**SC-4: Error fires on API errors, session limits, abnormal exits**
- `stop_api_rate_limit_error` — "I apologize, but I've hit the API rate limit" → Error
- `stop_session_limit_error` — "The session limit has been reached" → Error
- `stop_context_window_error` — "context window exceeded, cannot continue" → Error
- `stop_authentication_failed_error` — "authentication failed for the API" → Error

**SC-5: Each type has visually distinct styling (validated via body_text output)**
- `body_text_task_complete_contains_checkmark` — body for TaskComplete contains "✔"
- `body_text_permission_uses_message` — body for PermissionRequest uses input.message
- `body_text_question_uses_last_message` — body for Question uses input.last_assistant_message
- `body_text_error_uses_last_message` — body for Error uses input.last_assistant_message
</action>
<acceptance_criteria>
- tests/notification_types.rs exists
- tests/notification_types.rs contains `fn stop_normal_completion_is_task_complete`
- tests/notification_types.rs contains `fn notification_permission_prompt_type`
- tests/notification_types.rs contains `fn stop_question_ends_with_question_mark`
- tests/notification_types.rs contains `fn stop_api_rate_limit_error`
- `cargo test --test notification_types` exits 0 with all tests passing
</acceptance_criteria>
</task>

<task id="2">
<title>Asset extraction integration tests</title>
<read_first>
- src/assets.rs (ensure_hero_image, assets_dir functions)
</read_first>
<action>
Add integration tests to `tests/notification_types.rs` (or a separate `tests/assets.rs`):

1. `asset_extraction_creates_files` — call ensure_hero_image for each type with LOCALAPPDATA set to a temp dir (use std::env::set_var in test), verify files are created
2. `asset_extraction_skips_existing` — create a dummy file first, call ensure_hero_image, verify the original file is NOT overwritten (compare file content/mtime)
3. `asset_extraction_returns_none_on_invalid_dir` — set LOCALAPPDATA to an invalid path, verify ensure_hero_image returns None (graceful degradation)
4. `extracted_files_are_valid_png` — verify first 8 bytes of extracted file match PNG magic number `[137, 80, 78, 71, 13, 10, 26, 10]`

Note: Tests that modify env vars should use a mutex or run with `--test-threads=1` to avoid race conditions. Alternatively, add a `ensure_hero_image_in_dir(dir: &Path, ntype)` variant that accepts an explicit directory for testability.
</action>
<acceptance_criteria>
- Tests file contains `fn asset_extraction_creates_files`
- Tests file contains `fn extracted_files_are_valid_png`
- `cargo test` exits 0
</acceptance_criteria>
</task>

<task id="3">
<title>Add testability to assets module (explicit dir parameter)</title>
<read_first>
- src/assets.rs (current ensure_hero_image function)
</read_first>
<action>
Refactor `src/assets.rs` for testability:

1. Add internal function `pub fn ensure_hero_image_in(dir: &Path, ntype: NotificationType) -> Option<PathBuf>`:
   - Same logic as ensure_hero_image but takes explicit directory
   - This is the core logic function

2. Make `pub fn ensure_hero_image(ntype: NotificationType) -> Option<PathBuf>` a thin wrapper:
   ```rust
   pub fn ensure_hero_image(ntype: NotificationType) -> Option<PathBuf> {
       ensure_hero_image_in(&assets_dir(), ntype)
   }
   ```

This allows integration tests to pass a temp directory without mutating env vars.
</action>
<acceptance_criteria>
- src/assets.rs contains `pub fn ensure_hero_image_in(`
- src/assets.rs `ensure_hero_image` delegates to `ensure_hero_image_in`
- `cargo check` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
cargo test && cargo build --release
```

- All unit and integration tests pass
- Release binary size check: `(Get-Item target\release\claude-win-notify.exe).Length -lt 15MB`

## must_haves

- Integration tests cover all 5 ROADMAP success criteria
- Asset extraction is testable without side effects (explicit dir param)
- Graceful degradation tested (missing assets, invalid paths)
- All 4 notification types have distinct classification paths tested
- Bug #11964 fallback (D-19) explicitly tested

## truths

- D-07: Unknown hook events → silent exit(0) (tested by existing hook.rs tests)
- D-09: Subsequent runs skip extraction (existence check)
- D-13: stop_hook_active == true → no notification
